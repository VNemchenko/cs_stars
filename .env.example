PORT=3010
# If true, the server will actually execute `docker run ...`.
ENABLE_DOCKER_RUN=false

# Fallback image if an egg does not define one
DEFAULT_DOCKER_IMAGE=alpine:3.19

# Optional: mapping from egg slug to Pterodactyl egg id (not used by default flow)
# PTERO_EGG_ID_MAP={"cs-1-6-rehlds":123,"counter-strike-global-offensive":456}

